# Kubernetes Plan

The `k8s/` directory will contain simple Kubernetes manifests for AKS.

Planned Kubernetes concepts:

- Deployment
- Service
- ConfigMap
- Secret
- Health checks
- Ingress or external exposure

Secrets will not be hardcoded in YAML files.

