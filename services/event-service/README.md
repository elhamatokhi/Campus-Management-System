# Event Service

Node.js + Express microservice for campus event management and event image references.

This service will be implemented in a later phase.

Planned endpoints:

- `GET /health`
- `GET /api/events`
- `GET /api/events/:id`
- `POST /api/events`
- `PUT /api/events/:id`
- `DELETE /api/events/:id`

