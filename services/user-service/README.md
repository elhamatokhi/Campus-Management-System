# User Service

Node.js + Express microservice for users, authentication, profiles, and roles.

This service will be implemented in a later phase.

Planned endpoints:

- `GET /health`
- `POST /api/users/register`
- `POST /api/users/login`
- `GET /api/users/me`
- `PUT /api/users/me`

